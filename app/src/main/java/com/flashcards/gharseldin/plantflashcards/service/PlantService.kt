package com.flashcards.gharseldin.plantflashcards.service

import com.flashcards.gharseldin.plantflashcards.dto.Plant

/**
 * Created by Amr Gharseldin on 4/23/18.
 */

class PlantService {

    fun parsePlantsFromJsonData(difficulty: String?): List<Plant>?{

        var allPlants = ArrayList<Plant>()

        var i = 1 + 1

        var plant = Plant()
        // Open connection to data feed

        // Parse to plant objects

        // Add plant objects to a collection

        // return the results
        return allPlants;
    }
}